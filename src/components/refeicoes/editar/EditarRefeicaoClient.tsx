import { Refeicao } from "@/models/db-types/Refeicao"

export default function EditarRefeicaoClient({refeicao}: {refeicao: Refeicao}) {
    console.log(refeicao)
    
    return (
        <div>

        </div>
    )
}